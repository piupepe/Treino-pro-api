services:
  - type: web
    name: treino-pro-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9

      - key: ANTHROPIC_API_KEY
        sync: false
